<Controls:MetroWindow x:Class="LB_Common.Forms.MessagePrompt"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        xmlns:local="clr-namespace:LB_Common.Forms"
        mc:Ignorable="d"
        Title="Placeholder" Height="Auto" SizeToContent="WidthAndHeight" Width="Auto" MinWidth="300" ShowInTaskbar="True" ResizeMode="NoResize" 
                      WindowStartupLocation="CenterScreen" WindowStyle="ToolWindow" TitleCharacterCasing="Normal" Name="Window"
                      PreviewKeyDown="Window_PreviewKeyDown">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition MinHeight="70" Height="Auto"/>
            <RowDefinition Height="Auto" />
            <RowDefinition MinHeight="50" Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel x:Name="helpTestStackpanel" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <iconPacks:PackIconMaterialLight x:Name="icon" Kind="AlertCircle"  Margin="20,10,10,10" Foreground="OrangeRed" Height="33" Width="33" VerticalAlignment="Center"/>
            <TextBlock x:Name="helpTextBlock" Margin="10" Text="Placeholder text. This will be replaced in code" TextWrapping="Wrap" FontSize="12" Width="Auto" MinWidth="100" MaxWidth="380" Height="Auto" MaxHeight="800" VerticalAlignment="Center"/>
        </StackPanel>

        <Grid Grid.Row="1" HorizontalAlignment="Center">
            <RichTextBox x:Name="richTextBox" IsReadOnly="True" Margin="15,5,15,0" Height="150" MinWidth="350" MaxWidth="500" VerticalScrollBarVisibility="Visible">
                <FlowDocument>
                    <Paragraph>
                        <Run Text=""/>
                    </Paragraph>
                </FlowDocument>
            </RichTextBox>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button x:Name="okBtn" Margin="5" Content="OK" Width="Auto" MinWidth="75" Click="OK_Click"/>
            <Button x:Name="cancelBtn" Margin="5" Content="Cancel" Width="Auto" MinWidth="75" Click="Cancel_Click"/>
            <Button x:Name="yesBtn" Margin="5" Content="Yes" Width="Auto" MinWidth="75" Click="Yes_Click"/>
            <Button x:Name="noBtn" Margin="5" Content="No" Width="Auto" MinWidth="75" Click="No_Click"/>
        </StackPanel>
    </Grid>
</Controls:MetroWindow>