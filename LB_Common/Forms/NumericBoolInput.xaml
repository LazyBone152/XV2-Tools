<Controls:MetroWindow x:Class="LB_Common.Forms.NumericBoolInput"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        xmlns:local="clr-namespace:LB_Common.Forms"
        mc:Ignorable="d"
        Title="{Binding ElementName=Window, Path=FormName}" Height="Auto" SizeToContent="Height" Width="377" ShowInTaskbar="True" ResizeMode="NoResize" 
                      WindowStartupLocation="CenterScreen" WindowStyle="ToolWindow" TitleCharacterCasing="Normal" Name="Window"
                      PreviewKeyDown="Window_PreviewKeyDown">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition MinHeight="50" Height="*"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition MinHeight="50" Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel x:Name="helpTestStackpanel" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0, 10">
            <iconPacks:PackIconMaterialLight Kind="Information"  Margin="10" Foreground="{DynamicResource MahApps.Brushes.Accent}" Height="33" Width="33" VerticalAlignment="Center"/>
            <TextBlock x:Name="helpTextBlock" Text="Placeholder text. This will be replaced in code" TextWrapping="Wrap" FontSize="12" Width="250" Height="Auto" MaxHeight="200" VerticalAlignment="Center"/>
        </StackPanel>
        
        <Grid Grid.Row="1" HorizontalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="150"/>
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0" x:Name="valueLabel" Content="Value" HorizontalContentAlignment="Right" Width="100" Height="25"/>
            <Controls:NumericUpDown Grid.Column="1" x:Name="valueControl" Minimum="0" Maximum="50" Height="20" Width="140" />
        </Grid>
        
        <Grid Grid.Row="2" HorizontalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="150"/>
            </Grid.ColumnDefinitions>
            <Label x:Name="boolLabel" Content="Bool" HorizontalContentAlignment="Right"/>
            <CheckBox Grid.Column="1" IsChecked="{Binding ElementName=Window, Path=BoolValue, UpdateSourceTrigger=PropertyChanged}" Margin="5,0,0,0" VerticalAlignment="Center" HorizontalAlignment="Left" Width="20"/>
        </Grid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button Margin="2.5" Content="OK" HorizontalAlignment="Left" VerticalAlignment="Top" Width="75" Click="OK_Click"/>
            <Button Margin="2.5" Content="Cancel" HorizontalAlignment="Left" VerticalAlignment="Top" Width="75" Click="Cancel_Click"/>
        </StackPanel>
        
    </Grid>
</Controls:MetroWindow>
